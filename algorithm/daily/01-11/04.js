// 입력되는 달(month)에 따라 각 달에 며칠이 있는지 보여주는 함수를 만들려고 합니다.
// 각 조건에 해당하는 알맞은 값을 입력해주세요.


function temperature(num) {
	if(num >= 27) {
    return ("조급 덥습니다")
	} else if (num >= 23) {
    return ("날씨가 좋네요")
  } else {
    return ("조금 춥네요")
  }
}

import styled from "@emotion/styled";

export const Table = styled.table`
  width: 1200px;
  height: 583px; 
  border-top: 1px solid black;
  border-bottom: 1px solid black;
  border-collapse: collapse;
`

export const TableThNum = styled.th`
  height: 52px;
  font-weight: 500;
  font-size: 18px;
  width: 34px;
`

export const TableThTitle = styled.th`
  font-weight: 500;
  font-size: 18px;
  width: 300px;
`

export const TableThWriter = styled.th`
  font-weight: 500;
  font-size: 18px;
  width: 50px;

`
export const TableThDate = styled.th`
  font-weight: 500;
  font-size: 18px;
  width: 80px;
`

export const TableTr = styled.tr`
  text-align: center;
`

export const TableTrList = styled.td`
  height: 52px;
  font-weight: 400;
  font-size: 16px;
  color: #4F4F4F;
  border-top: 1px solid #BDBDBD;
`
